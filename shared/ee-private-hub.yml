# Copy these blocks into any execution-environment.yml that uses the private hub.
# Run from repo root before build:  bash scripts/write-hub-ansible-cfg.sh
#
# additional_build_files:
#   - src: ../files/ansible.cfg
#     dest: configs
#
# additional_build_steps:
#   prepend_galaxy:
#     - RUN mkdir -p /etc/ansible
#     - COPY _build/configs/ansible.cfg /etc/ansible/ansible.cfg
